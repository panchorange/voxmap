"""Vendored neural network models (torch.nn.Module).

Each model file documents its origin (upstream repo + commit) at the top.
Adapters in `voxmap.{segmentation,embedding}` consume these models and
satisfy the corresponding Protocol.

See [[docs/design/decisions/2026-05-10_vendor_pyannote_models]] for rationale.
"""
